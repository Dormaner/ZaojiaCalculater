# 工程造价计算器 —「工程可研费」模块执行计划

> 参考模板：造价咨询费 / 工程监理费（5 步向导 + 统一测算报告）
> 依据文件：
> 1. `湖南省：关于转发《国家计委关于印发建设项目前期工作咨询收费暂行规定的通知》的通知.pdf`（〔2000〕湘价房字第 95 号）
> 2. `浙江省物价局关于公布降低后的编制和评估可行性研究报告等建设项目前期咨询收费的通知.pdf`（浙价服〔2013〕252 号）
> 3. `广西建设项目投资额3000万元以下分档收费标准—桂价经字[2000]88号.pdf`
> 4. 三文共同引用的上位文件：**计价格〔1999〕1283 号**《建设项目前期工作咨询收费暂行规定》

---

## 一、现状与问题

当前「工程可研费」子页（`App.tsx` `calculatorSubTab === 'feasibility'`，约 2049 行）实现非常简陋：

```tsx
估算投资额 (万元)  →  工程所属行业（复用 selectedIndustry）  →  [开始测算可研费]
```

问题清单：

| # | 问题 | 说明 |
|---|---|---|
| 1 | 只有一屏，无省份概念 | 直接套用国家表，不区分湖南 80%、浙江降低 20%、广西附表 |
| 2 | 复用设计费的行业系数 | `handleCalculate` 中把 `industryFactor`(1.05) 同时传给设计费与可研费；可研应使用**行业调整系数**（0.7～1.3） |
| 3 | 3000 万以下档位是**编造值** | `FEASIBILITY_FEE_CONFIG.rates` 中 `f(0)=1、f(500万)=2` 无出处；各地文件均有专门附表 |
| 4 | 4 个项目**强制全部求和** | 实际项目通常只需测算其中 1～2 项，无法选择 |
| 5 | 缺 `评估咨询初步设计文件` | 浙江第 5 项、广西「按可研评估执行」 |
| 6 | 缺「经济适用住房」项目类型 | 湖南附表二 |
| 7 | 缺工程复杂程度调整系数（0.8～1.2）、财政拨款事业性质咨询公司 ×80% | 文件明确规定 |
| 8 | 无详细计算过程 | 与造价咨询费/监理费的报告规格不一致 |
| 9 | 报告页汇总卡片硬编码 4 项 | 与新版结果结构不匹配 |
| 10 | 无后端接口 | `server.ts` 没有可研分支 |

---

## 二、收费标准原始数据（已逐条校对，含连续性验证）

> **单位**：投资额 = 万元；费用 = 万元

### 2.1 国家计委 计价格〔1999〕1283 号 附件一（3000 万元以上）

| 咨询评估项目 | 3000万–1亿 | 1亿–5亿 | 5亿–10亿 | 10亿–50亿 | 50亿以上 |
|---|---|---|---|---|---|
| 一、编制项目建议书 | 6 – 14 | 14 – 37 | 37 – 55 | 55 – 100 | 100 – 125 |
| 二、编制可行性研究报告 | 12 – 28 | 28 – 75 | 75 – 110 | 110 – 200 | 200 – 250 |
| 三、评估项目建议书 | 4 – 8 | 8 – 12 | 12 – 15 | 15 – 17 | 17 – 20 |
| 四、评估可行性研究报告 | 5 – 10 | 10 – 15 | 15 – 20 | 20 – 25 | 25 – 35 |

**关键发现**：相邻档位的「上限」与「下限」严格相等（如 1 亿处：6 万档上限 28 = 下档下限 28），说明国家表本质是**一条连续折线**，可用「档内线性插值」还原。

### 2.2 湖南省 〔2000〕湘价房字第 95 号

- **第四条**：投资额 **3000 万元以上** → 国家表标准 **× 80%**
- **第四条**：投资额 **3000 万元以下** → 附表一
- **第五条**：财政拨款的事业性质工程咨询公司 → 上述标准 **× 80%**
- **第六条**：经济适用住房项目 → 附表二
- **第七条**：调整系数、工日费用标准按国家计委规定执行

附表一（3000 万元以下）：

| 咨询评估项目 | 500万以下 | 500万–1500万 | 1500万–3000万 |
|---|---|---|---|
| 一、编制项目建议书 | 0.8 – 1.6 | 1.6 – 2.4 | 2.4 – 4.8 |
| 二、编制可行性研究报告 | 1.6 – 3.2 | 3.2 – 4.8 | 4.8 – 9.6 |
| 三、评估项目建议书 | 0.4 – 0.8 | 0.8 – 1.6 | 1.6 – 3.2 |
| 四、评估可行性研究报告 | 0.8 – 1.6 | 1.6 – 2.4 | 2.4 – 4 |

附表二（经济适用住房项目，固定值）：

| 咨询评估项目 | 3000万以下 | 3000万以上 |
|---|---|---|
| 一、编制项目建议书 | 0.6 | 1.0 |
| 二、编制可行性研究报告 | 0.9 | 1.5 |
| 三、评估项目建议书 | 0.4 | 0.5 |
| 四、评估可行性研究报告 | 0.6 | 1.0 |

> ✅ **连续性验证**：附表一在 3000 万处的取值分别为 4.8 / 9.6 / 3.2 / 4，
> 恰好等于「国家表 × 0.8」在 3000 万处的取值（6×0.8 / 12×0.8 / 4×0.8 / 5×0.8）。
> 故湖南全省可视为**一条连续曲线**：`< 3000万 用附表一`，`≥ 3000万 用国家表×0.8`。

### 2.3 浙江省 浙价服〔2013〕252 号

> 一、按 1283 号 + 浙价房〔1999〕411 号 规定的收费标准 **平均降低 20%**。
> 表格末尾：*以上收费按估算投资额分档计费，具体收费根据项目的复杂程度、工作要求和工作量由委托方双方在上述规定范围内协商确定。*

| 咨询评估项目 | 1000万以下 | 1000万–3000万 | 3000万–1亿 | 1亿–5亿 | 5亿–10亿 | 10亿–50亿 | 50亿以上 |
|---|---|---|---|---|---|---|---|
| 一、编制项目建议书 | 0.8 – 2 | 1.7 – 4.8 | 4 – 11.2 | 9.5 – 30 | 25 – 44 | 37.5 – 80 | 68 – 100 |
| 二、编制可行性研究报告 | 1.6 – 4 | 3.4 – 9.6 | 8 – 22.4 | 19 – 60 | 50 – 88 | 75 – 160 | 136 – 200 |
| 三、评估项目建议书 | 0.5 – 1.2 | 1 – 3.2 | 2.7 – 6.4 | 5.4 – 9.6 | 8 – 12 | 10 – 13.6 | 11.5 – 16 |
| 四、评估可行性研究报告 | 0.8 – 2 | 1.7 – 4 | 3.4 – 8 | 6.8 – 12 | 10 – 16 | 13.6 – 20 | 17 – 28 |
| 五、评估咨询初步设计文件 | 0.8 – 2 | 1.7 – 4 | 3.4 – 8 | 6.8 – 12 | 10 – 16 | 13.6 – 20 | 17 – 28 |

> ⚠️ **浙江表是「协商区间」，不是连续折线**（例：编制可研 3000 万处，上档上限 4.8 ≠ 本档下限 8）。
> 因此必须用「档内线性插值」模型统一处理（见第三章），档位边界处会有跳变，属文件本身特性。

### 2.4 广西 桂价经字〔2000〕88 号

- **第一条**：投资额 **3000 万元以下** → 本文附表
- **第二条**：**初步设计文件评估咨询，按可行性研究报告评估咨询收费标准执行**
- 投资额 3000 万元以上 → 国家 1283 号附件一（**无折扣**）

附表（3000 万元以下）：

| 咨询评估项目 | 500万以下 | 500万–1000万 | 1000万–3000万 |
|---|---|---|---|
| 一、编制项目建议书 | 1 – 1.5 | 1.5 – 2.5 | 2.5 – 6 |
| 二、编制可行性研究报告 | 2 – 4 | 4 – 6 | 6 – 12 |
| 三、评估项目建议书 | 0.5 – 1.0 | 1 – 1.5 | 1.5 – 4 |
| 四、评估可行性研究报告 | 1 – 1.5 | 1.5 – 2.5 | 2.5 – 5 |

> ✅ **连续性验证**：附表在 3000 万处 = 6 / 12 / 4 / 5，与国家附件一在 3000 万处完全一致 → 也是连续曲线。

### 2.5 调整系数（国家 1283 号 附件二）

**行业调整系数**（以国家表收费标准为 1）：

| 序号 | 行业 | 调整系数 |
|---|---|---|
| 1 | 石化、化工、钢铁 | 1.3 |
| 2 | 石油、天然气、水利、水电、交通（水运）、化纤 | 1.2 |
| 3 | 有色、黄金、纺织、轻工、邮电、广播、电视、医药、煤炭、火电（含核电）、机械（含船舶、航空、航天、兵器） | 1.0 |
| 4 | 林业、商业、粮食、建筑 | 0.8 |
| 5 | 建材、交通（公路）、铁道、市政公用工程 | 0.7 |

**工程复杂程度调整系数**：0.8 – 1.2（由工程咨询机构与委托单位协商确定）

**工日费用标准**（附件三，备用方法）：高级专家 1000–1200 元/工日；高级专业技术职称 800–1000；中级专业技术职称 600–800

---

## 三、计算模型

### 3.1 统一数据结构：档位区间表

三地（含国家表）统一用「**档位区间（下限 lo、上限 hi）+ 档内线性插值**」表达：

```ts
type Bracket = { min: number; max: number; lo: number; hi: number }; // 单位：万元，max 可为 Infinity
type FeeItem = { key: string; name: string; brackets: Bracket[] };
```

原有 `FEASIBILITY_FEE_CONFIG` 的「axis + rates 点列」模型无法表达浙江的协商区间，**予以废弃**。

### 3.2 档位判定规则

> **x ∈ (min, max]，首档含 x = 0**

即遍历档位，取第一个满足 `x <= max` 的档位。含两点考量：

- 湖南：「3000 万**以上**」不含 3000 万本身 → x = 3000 万应落入附表一第 3 档，得 4.8 万（而非国家档 9.6 万）✅ 与文件一致
- 广西：x = 3000 万时分属两表值相同（6 万），无歧义 ✅

### 3.3 档内插值与最终费用

```text
定位比例 t = (x − min) / (max − min)          // 顶档（max = Infinity）时 t 取 1
基准费用   = lo + (hi − lo) × t               // 单位：万元
最终费用   = 基准费用 × 行业调整系数 × 工程复杂程度系数 × 其他系数
```

> **说明**：对国家/湖南/广西这类连续表，该式与「按折线插值」结果**完全等价**
> （验证：国家编制可研 3 亿元 → 28 + (75−28)×0.5 = 51.5 万元，与折线插值一致 ✅）。
> 对浙江的协商区间，该式给出「按投资额在档内位置线性取值」的确定性结果。

### 3.4 顶档（50 亿元以上）取值

国家表为 `100–125 / 200–250 / 17–20 / 25–35`，浙江为 `68–100 / 136–200 / 11.5–16 / 17–28`，均无投资额上限。
**处理：取该档上限（hi）作为封顶值**（湖南 = 国家×0.8 → 200 万；浙江 → 200 万），并在报告中注明「已达最高档，按该档收费上限封顶」。

### 3.5 系数适用范围

| 省份 | 行业调整系数 | 复杂程度系数 | 其他系数 |
|---|---|---|---|
| 湖南省 | ✅ 采用附件二 | ✅ 0.8–1.2 | ✅ 「财政拨款事业性质咨询公司」× 0.8（第五条） |
| 广西 | ✅ 采用附件二 | ✅ 0.8–1.2 | — |
| 浙江省 | 252 号未提及（视为已含在降低后的区间内）→ **提供选择但默认 1.0** | ✅ 0.8–1.2 | — |

### 3.6 经济适用住房（仅湖南）

附表二为**固定值**（非区间、不插值），直接取 `0.6 / 0.9 / 0.4 / 0.6`（≤3000 万）或 `1.0 / 1.5 / 0.5 / 1.0`（>3000 万），**不再叠加**行业/复杂程度系数，报告中注明依据附表二。

---

## 四、界面改造：5 步向导

沿用造价咨询费 / 监理费已确立的 5 步结构：

```ts
const FEASIBILITY_STEPS = [
  '1. 测算省份',
  '2. 咨询服务类型',
  '3. 计费基数',
  '4. 调整系数',
  '5. 测算报告',
];
```

> 容器使用普通 `<div className="contents">`（**不使用** `AnimatePresence mode="wait"`，规避已踩过的「退出节点不移除导致卡步」坑；`motion.div` 的入场动画保留）。

### 步骤 1 · 测算省份

卡片式选择：**湖南省 / 浙江省 / 广西壮族自治区**，附一句标准摘要（如「湖南省：3000 万以上按国家标准 80% 执行」）。选中后显示「继续」。

### 步骤 2 · 咨询服务类型（多选）

以 `<optgroup>` 分组的多选面板，**可同时勾选多项**（替换现在「4 项强制全选求和」）：

```
【一般建设项目】
  ☐ 编制项目建议书
  ☐ 编制可行性研究报告          ← 默认勾选
  ☐ 评估项目建议书
  ☐ 评估可行性研究报告
  ☐ 评估咨询初步设计文件          ← 仅浙江、广西显示
【经济适用住房项目】              ← 仅湖南显示
  ☐ 编制项目建议书
  ☐ 编制可行性研究报告
  ☐ 评估项目建议书
  ☐ 评估可行性研究报告
```

- 未勾选任何一项时「继续」置灰
- 已勾选项在步骤 5 报告中逐项列示并给出「合计」

### 步骤 3 · 计费基数

- 输入框：**估算投资额（项目建议书/可行性研究报告的估算投资额）**，单位 **万元**
- 实时提示命中的档位，例如：`6000 万元 ∈ 3000万元–1亿元 档`
- 校验：> 0 才可继续

### 步骤 4 · 调整系数

| 控件 | 说明 |
|---|---|
| 行业调整系数（下拉） | 5 档 + 「自定义」，显示系数值 |
| 工程复杂程度调整系数（输入 0.8–1.2，默认 1.0） | 越界时裁剪并提示 |
| 其他调整系数（输入，默认 1.0） | 湖南预置快捷项「财政拨款事业性质咨询公司（×0.8）」 |
| 综合调整系数（只读展示） | 三者乘积，实时联动 |

> 选中「经济适用住房」类项目时，本步整体置灰并提示「经济适用住房项目按附表二固定标准计取，不适用调整系数」。

### 步骤 5 · 测算报告

结果卡片 + 「重新测算 / 复制报告」，与造价咨询费、监理费样式一致。

---

## 五、报告模板（按湖南模板风格，含详细计算过程）

```text
【建设项目前期工作咨询费测算结果 (湖南省标准)】
1. 咨询服务类型：编制可行性研究报告
2. 计费基数 (估算投资额)：6000 万元
3. 调整系数：行业调整系数 0.80（第4类：林业、商业、粮食、建筑）; 工程复杂程度系数 1.00; 其他系数 1.00
   —— 综合调整系数 = 0.80 × 1.00 × 1.00 = 0.8000
4. 收费标准依据：〔2000〕湘价房字第95号 第四条 / 计价格〔1999〕1283号 附件一（3000万元以上按 80% 执行）
——————————————————
计算过程说明：
5. 计费档位（估算投资额，万元）：0–500 ｜ 500–1500 ｜ 1500–3000 ｜ 3000–10000 ｜ 10000–50000 ｜ 50000–100000 ｜ ≥100000
6. 按档内插入法计算基准费用：
   估算投资额 6000 万元位于「3000万元–1亿元」档，该档收费区间为 9.6 – 22.4 万元
   = 国家表区间 12 – 28 万元 × 80%（湘价房字第95号 第四条）
   定位比例 = (6000 − 3000) / (10000 − 3000) = 42.8571%
   基准费用 = 9.6 + (22.4 − 9.6) × 42.8571% = 15.0857 万元
7. 计算式：15.0857 万元 × 0.8000 = 12.0686 万元（¥120,686 元）
——————————————————
建设项目前期工作咨询费用(合计)：¥120,686 元
备注：估算投资额是指项目建议书或者可行性研究报告的估算投资额；具体收费标准根据估算投资额在相对应的区间内用插入法计算，再乘以行业调整系数与工程复杂程度调整系数。本表未计工程勘察、试验及优质优价等另行协商费用。
```

多选时第 1、6、7 项按项目分条列出，末尾追加：

```text
——————————————————
建设项目前期工作咨询费用(合计)：¥350,000 元
（编制项目建议书 ¥120,686 + 编制可行性研究报告 ¥229,314）
```

---

## 六、多省份示例结果（供验收对照）

以 **估算投资额 6000 万元、编制可行性研究报告、综合系数 1.0** 为例：

| 省份 | 命中档位 | 档内区间(万元) | 定位比例 | 基准费用 | 说明 |
|---|---|---|---|---|---|
| 湖南省 | 3000万–1亿 | 9.6 – 22.4 | 42.8571% | **15.0857 万元** | 国家 12–28 × 80% |
| 广西 | 3000万–1亿 | 12 – 28 | 42.8571% | **18.8571 万元** | 国家表全额 |
| 浙江省 | 3000万–1亿 | 8 – 22.4 | 42.8571% | **14.1714 万元** | 252 号降低后表 |

以 **600 万元** 为例（考验 3000 万以下附表）：

| 省份 | 命中档位 | 档内区间(万元) | 定位比例 | 基准费用 |
|---|---|---|---|---|
| 湖南省 | 500万–1500万 | 3.2 – 4.8 | 10% | **3.3600 万元** |
| 广西 | 500万–1000万 | 4 – 6 | 20% | **4.4000 万元** |
| 浙江省 | 1000万以下 | 1.6 – 4 | 60% | **3.0400 万元** |

以 **湖南 6000 万元 + 行业 0.8 + 复杂程度 1.1** 为例：`15.0857 × 0.8 × 1.1 = 13.2754 万元`

---

## 七、代码改造清单

| 文件 | 动作 |
|---|---|
| `src/data/feasibilityStandards.ts` | **新建**。单一数据源：`FEASIBILITY_STANDARDS`（湘/浙/桂）、`FEASIBILITY_STEPS` 文案、行业调整系数表、`FEASIBILITY_BILLING_BASE_LABEL`、`locateBracket()`、`calculateFeasibilityItems()`（含报告生成）、`FEASIBILITY_PROVINCES` |
| `src/App.tsx` | 删除 `FEASIBILITY_FEE_CONFIG`；新增 `feasibilityStep / feasibilityProvince / feasibilityServiceKeys / feasibilityAmount / feasibilityIndustry / feasibilityComplexity / feasibilityOtherFactor / feasibilityResultText / showFeasibilityResult` 等 state 与 `resetFeasibilityWizard()`；将 `calculatorSubTab === 'feasibility'` 分支（约 2049–2090 行）替换为 5 步向导（结构对齐 supervision 分支）；改造报告页可研卡片（约 2951 行）为展示向导结果；`handleCalculate` 中的可研部分解耦（不再传 `industryFactor`）；`FeeSection title="工程可研费规范"` 的 `items` 改为三地费率表 |
| `server.ts` | 新增 `import ... from './src/data/feasibilityStandards'`；新增 `POST /api/calculate-feasibility`（入参：province / items / amount / 系数；出参：`{ total, details, reportText }`） |
| `src/services/calculationService.ts` | 删除已废弃的 `calculateFeasibilityFee / calculateFeasibilityReportFee / ...`；顺带删除已无引用的 `calculateSupervisionFee` |
| `src/types.ts` | `CalculationRecord` 等类型不变；如需可为可研结果补类型定义（可选） |

### 复用的既有工具

- `formatNum`、`buildTierLabels` 风格、报告「分隔线 + 备注」排版
- 「后端优先 + 本地回退」调用模式（与造价咨询费、监理费一致）
- 输入单位统一 **万元**；输出 ¥ 元 + 万元双写

---

## 八、需要你确认的 8 个问题

| # | 问题 | 我的建议 |
|---|---|---|
| 1 | 是否**直接替换**现有可研实现（不做旧版开关）？ | ✅ 替换（同监理费） |
| 2 | 步骤 2 用**多选**（可同时测算多项并合计）还是单选？ | ✅ 多选 |
| 3 | 档内取值是否统一用**线性插值**（浙江的协商区间也按此处理）？ | ✅ 是；不额外加「档内浮动」滑杆 |
| 4 | 顶档（≥50 亿元）取**上限封顶**？ | ✅ 是 |
| 5 | **经济适用住房**（仅湖南附表二）是否实现？ | ✅ 实现，作为步骤 2 的一个分组 |
| 6 | **工日费用法**（附件三，元/工日）是否实现？ | ❌ 不实现，仅在报告备注中提及（同监理费的处理） |
| 7 | 浙江是否也提供**行业调整系数**选择？ | ✅ 提供，默认 1.0（252 号原文未规定） |
| 8 | 是否新增后端接口 `/api/calculate-feasibility` 并共用 `src/data/feasibilityStandards.ts`？ | ✅ 是（避免重复数据这个反复出问题的坑） |

补充（无需回答，仅在实施时执行）：
- 顺带删除 `src/services/calculationService.ts` 中已无引用的 `calculateSupervisionFee`
- 报告页可研卡片按新版结果结构重做（不再硬编码 4 项）

---

## 九、验收方案

1. **后端接口**：`POST /api/calculate-feasibility` 跑通上表 6 个示例，数值逐一比对
2. **前端本地回退**：关闭 3001 端口后重复同样用例，结果一致
3. **浏览器端到端**：完整走完 5 步（湘/浙/桂 各 1 次，含 1 次多选、1 次勾选行业 0.8、1 次 3000 万以下档）
4. **边界用例**：估算投资额 = 500 / 1500 / 3000 / 10000 / 500000 万元（档位边界）、超顶档、行业自定义系数
5. `npx tsc --noEmit` 通过（过滤既有 `@types/react` 告警）
6. 控制台无报错
