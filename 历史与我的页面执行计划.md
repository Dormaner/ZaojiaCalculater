# 工程造价计算器 ——「历史」与「我的」页面执行计划

> 前置状态：「计算器」页面（4 个子模块：造价咨询费 / 工程可研费 / 工程监理费 / 工程设计费）已完成并归档（commit `551d55e`）。
> 本计划只覆盖「历史」「我的」两个页面，**不改动「计算器」页面的计算逻辑与报告格式**。

---

## 一、目标

| 页面 | 目标 |
|---|---|
| **历史** | 把「计算器」页面各模块**已测算过的结果**持久化，用**一张张卡片**呈现；点击卡片 → 打开对应的**测算报告全文**（可复制、可删除、可回填重算） |
| **我的** | 为「计算器」页面的 **4 个子模块设置默认省份**；**取消登录功能**（删除账号相关 UI） |

---

## 二、现状（需要改造的点）

### 2.1 「历史」页面（`src/App.tsx` 约 4748–4805 行）
- 数据源是**写死的 mock 数据** `MOCK_RECORDS`（`src/App.tsx` 第 108 行，5 条假记录）
- 类型 `CalculationRecord`（`src/types.ts` 第 6 行）只有 `id / title / date / totalAmount / type`，**没有报告全文**
- 卡片点击后只是 `setActiveTab('report')`，跳到**汇总页**，**不展示该条记录的报告**
- 搜索框是**装饰性的**（无 `value` / `onChange`），不生效
- 删除按钮是空的（`/* Delete logic */`）
- 没有空状态、没有模块筛选、没有持久化

### 2.2 「我的」页面（约 4808–5680 行）
- **账号相关（需删除）**：
  - 顶部用户资料卡（头像图 + 「工程造价师」+ 「执业证书编号: CE-2023089412」+ Pro 标签）→ 点击进入「账号设置」二级页
  - 「账号设置」二级页（`showAccountSettings`）：更换头像、登录密码、手机绑定、实名认证
  - 「退出登录」按钮（红色）
- **标准规范配置（保留）**：`计算偏好`（`globalPresets` 偏好方案下拉、`FeeSection` 造价咨询费规范/可研费规范/监理费规范/设计费规范）—— 这是各标准的**只读查阅 + 逐档编辑**，与登录无关，保留
- **缺少**：默认省份设置

### 2.3 「计算器」页面省份状态的现状（默认省份要作用于这 4 处）
| 子模块 | 状态变量 | 当前初值 | 可选省份 |
|---|---|---|---|
| 造价咨询费 | `consultingProvince` | `'湖南省'` | 湖南省 / 浙江省 / 贵州省 / 用户自定义省份（`customProvinces`） |
| 工程可研费 | `feasibilityProvince` | `'湖南省'` | `FEASIBILITY_PROVINCES`（湖南省 / 浙江省 / 广西壮族自治区） |
| 工程监理费 | `supervisionProvince` | `'湖南省'` | `SUPERVISION_PROVINCES` |
| 工程设计费 | `designProvince` | `'全国'` | `全国` / `DESIGN_PROVINCE_NAMES`（31 省）/ 用户自定义省份（`customDesignProvinces`） |

---

## 三、数据结构与存储

### 3.1 历史记录模型（新增到 `src/types.ts`）

```ts
/** 计算器各子模块 */
export type HistoryModule = 'design' | 'feasibility' | 'supervision' | 'consulting';

export interface HistoryRecord {
  /** 唯一 id：`${Date.now()}-${随机 4 位}` */
  id: string;
  module: HistoryModule;
  /** 模块中文名，如「工程设计费」 */
  moduleName: string;
  /** 主标题：取关键参数摘要，如「建筑、市政、电信工程」 */
  title: string;
  /** 副标题：如「计费额 3,000 万元 · Ⅱ级（较复杂）」 */
  subtitle: string;
  /** 测算省份（用于卡片角标与筛选） */
  province: string;
  /** 计费额 / 计费基数（万元） */
  amount: number;
  /** 总费用（万元，保留 4 位小数） */
  totalWan: number;
  /** 总费用（元，整数） */
  totalYuan: number;
  /** 创建时间戳（毫秒），卡片显示格式化后的本地时间 */
  createdAt: number;
  /** 报告全文（就是现在「测算报告」页展示的那段文本） */
  reportText: string;
  /** 入参快照，用于「重新测算」回填（可空，见 §8 决策点） */
  payload?: Record<string, unknown>;
}
```

- 旧的 `CalculationRecord` 与 `MOCK_RECORDS` **删除**（无其它引用）

### 3.2 localStorage 键位

| 键 | 内容 | 说明 |
|---|---|---|
| `cost_calculator_history` | `HistoryRecord[]` | 历史记录，**按 `createdAt` 倒序**，**上限 200 条**（超出丢弃最旧） |
| `cost_calculator_default_provinces` | `{ consulting, feasibility, supervision, design }` | 4 个模块的默认省份 |

- 读写封装为工具函数，统一 `try/catch`（localStorage 配额/隐私模式异常不得导致白屏）：
  - `loadHistory(): HistoryRecord[]`
  - `saveHistory(list): void`
  - `pushHistory(record: Omit<HistoryRecord, 'id' | 'createdAt'>): HistoryRecord[]`
  - `deleteHistory(id): HistoryRecord[]`
  - `clearHistory(): void`
  - `loadDefaultProvinces()` / `saveDefaultProvinces(cfg)`

---

## 四、「历史」页面设计

### 4.1 记录写入时机

在「计算器」4 个模块的测算成功回调里，拿到 `reportText` **之后**追加一条记录：

| 模块 | 写入位置 | `title` 取值 | `subtitle` 取值 |
|---|---|---|---|
| 工程设计费 | `handleCalculateDesign()` 两条分支（后端返回 / 本地回退）合并处 | `getDesignCategoryName()` | `计费额 ${amount} 万元 · ${complexity.name}` |
| 工程可研费 | `handleCalculateFeasibility()` | 所选的咨询服务类型（多选时用「、」连接，超过 2 项折叠为「XX 等 N 项」） | `计费基数 ${amount} 万元` |
| 工程监理费 | `handleCalculateSupervision()` | 在建工程类别名（`getSupervisionCategoryName()`） | `计费基数 ${amount} 万元 · 难度系数 ${factor}` |
| 造价咨询费 | `handleCalculate()` 对应分支 | 服务类型名（如「全过程造价咨询服务 A 型」） | `计费基数 ${amount} 万元` |

- `totalWan` / `totalYuan`：从 `reportText` 用现有的 `extractFeeOnly()` 提取（已有函数，报告页在用），**不再单独维护**
- 省份：分别取 `designProvince` / `feasibilityProvince` / `supervisionProvince` / `consultingProvince`
- 同一参数重复测算**不去重**（每次测算都是一条新记录），但**同一秒内完全相同的 `reportText` 视为重复，跳过写入**（防止连点两次按钮产生重复卡片）

### 4.2 卡片列表 UI

```
┌──────────────────────────────────────────────┐
│  历史                                         │
│  [搜索框：搜索工程类别 / 省份 / 服务类型]      │
│                                              │
│  [全部 12] [设计费 4] [可研 3] [监理 2] [咨询 3]   ← 模块筛选 chip
│                                              │
│  ┌──────────────┐ ┌──────────────┐           │
│  │ 图标  工程设计费│ │ 图标  造价咨询费│   ...     │  1 列(手机) / 2 列(平板)
│  │ 建筑、市政、电信  │ │ 全过程咨询 A 型 │           │  / 3 列(桌面)
│  │ 计费额 3000万·Ⅱ级│ │ 计费基数 6000万 │           │
│  │ 湖南省   2026-09-21 15:04 │ ...      │           │
│  │ 总费用  ¥1,193,700 │           │
│  └──────────────┘ └──────────────┘           │
└──────────────────────────────────────────────┘
```

- **模块色标**（沿用现有配色，前端常量映射）：
  - 工程设计费 `#007AFF`（蓝）/ 图标 `Layers`
  - 工程可研费 `#34C759`（绿）/ 图标 `BookOpen`
  - 工程监理费 `#FF9500`（橙）/ 图标 `Eye`
  - 造价咨询费 `#AF52DE`（紫）/ 图标 `BarChart3`
- 每张卡片：左侧色条 + 图标 + 模块名；主标题（`title`，`truncate`）；副标题（`subtitle`）；底部左侧「省份 · 时间」，右侧「总费用 `¥xxx 元`」
- 右上角 `Trash2` 删除（`e.stopPropagation()`），删除后弹出 **3 秒可撤销** 的轻提示（撤销则恢复）
- **点击卡片** → 打开报告详情浮层（见 4.4）
- **空状态**：居中提示「暂无测算记录」+「前往计算器 ⇒」按钮
- **筛选 chip**：`全部 / 工程设计费 / 工程可研费 / 工程监理费 / 造价咨询费`，显示各自条数
- **搜索**：对 `title`、`subtitle`、`province`、`moduleName` 做**不区分大小写的包含匹配**（用 `String.prototype.includes`，本插件按 UTF-16 比较，中文与数字均可用）
- 替换掉现在这个装饰性 `Search` 输入框，改为受控 `historyKeyword`

### 4.3 排序
固定按 `createdAt` **倒序**（最新在最上）。不做用户可切换排序，保持简洁。

### 4.4 报告详情浮层

复用「计算器」第 5 步 / 汇总页现有的 `<pre className="whitespace-pre-wrap ...">` 报告样式，做成**全屏浮层**：

```
┌──────────────────────────────────────┐
│ ← 工程设计费测算报告        [复制] [删除] │
├──────────────────────────────────────┤
│  ¥1,193,700 元                        │
│  ──────────────────────────────       │
│  【工程设计费测算结果 (国家标准)】       │
│  1. 工程类别：建筑、市政、电信工程…     │
│  …（reportText 全文，可滚动）           │
├──────────────────────────────────────┤
│      [ 重新测算 ]   [ 关闭 ]            │
└──────────────────────────────────────┘
```

- **复制**：`navigator.clipboard.writeText(reportText)`，失败时回退到 `document.execCommand('copy')`（Capacitor WebView 兼容），成功后按钮文案临时变「已复制」
- **删除**：删除该条并关闭浮层
- **重新测算**：
  - 切换到「计算器」页 + 对应子模块，**回填该记录的入参快照**，回到第 1 步（或直接跳到第 4 步「阶段与调整系数」），让用户改完参数后重新生成
  - 若记录没有 `payload`（历史遗留 / 快照版本不匹配），此按钮降级为「前往对应模块」纯跳转
- 关闭：点遮罩 / 点右上角 `X` / `Esc`

### 4.5 与「汇总页」的关系
**不改动**汇总页（`activeTab === 'report'`）。历史页是**独立入口**，只展示历史记录自己的报告全文；汇总页继续展示「最近一次测算」的汇总卡片与跳转链接。

---

## 五、「我的」页面设计

### 5.1 删除登录/账号相关 UI
| 删除项 | 位置 |
|---|---|
| 用户资料卡（头像 + 「工程造价师」+ 执业证书编号 + Pro 标签） | 「我的」主页顶部 `section` |
| 「账号设置」二级页整体（`showAccountSettings === true` 分支） | 更换头像 / 登录密码 / 手机绑定 / 实名认证 |
| 「退出登录」按钮 | 页面底部红色按钮 |
| 状态 `showAccountSettings` 及其 `AnimatePresence` 包裹 | 简化为一层直接渲染 |
| `MOCK` 头像外链 `lh3.googleusercontent.com/...` 图片 | 一并移除（外部资源，离线不可用） |

> 图标 `Lock` / `LogOut` / `ArrowLeft` / `User` 等：删除后逐个核查是否还有其它引用，无引用则从 `lucide-react` 导入列表中移除。

### 5.2 新增「默认省份」设置卡片

放在页面**最上方**（替代原用户资料卡位置）：

```
┌──────────────────────────────────────────────┐
│  ⚙ 默认省份设置                     [恢复默认] │
│  打开计算器时各模块自动选中的省份                │
│                                              │
│  造价咨询费   [湖南省            ▾]           │
│  工程可研费   [湖南省            ▾]           │
│  工程监理费   [湖南省            ▾]           │
│  工程设计费   [全国              ▾]           │
└──────────────────────────────────────────────┘
```

- 4 个 `<select>`，选项来源：
  - 造价咨询费：`湖南省 / 浙江省 / 贵州省` + `customProvinces.map(p => p.name)`
  - 工程可研费：`FEASIBILITY_PROVINCES`
  - 工程监理费：`SUPERVISION_PROVINCES`
  - 工程设计费：`全国` + `DESIGN_PROVINCE_NAMES` + `customDesignProvinces.map(p => p.name)`
- 选择即保存（`saveDefaultProvinces`），显示「已保存」轻提示
- **「恢复默认」**按钮：重置为 `{ consulting: '湖南省', feasibility: '湖南省', supervision: '湖南省', design: '全国' }`
- 自定义省份被删除时，若它是某模块的默认省份 → 自动回落到该模块的第一个内置省份（在 `removeCustomProvince` / `removeCustomDesignProvince` 里补一处校验）

### 5.3 默认省份生效方式

- `src/App.tsx` 模块顶层加 `const DEFAULT_PROVINCES = loadDefaultProvinces();`
- 4 个 `useState` 初值改为：
  ```ts
  useState<string>(() => loadDefaultProvinces().consulting)   // 同理 feasibility / supervision / design
  ```
  （用惰性初始化，避免每次渲染都读 localStorage）
- 用户在「我的」页改动默认省份后**立即写入 localStorage**；**已在计算器中选择的省份不强制改**（避免打断当前操作），下次启动生效

### 5.4 保留不动的部分
- 「计算偏好」区块：`globalPresets` 偏好方案（保存/应用/删除）、`FeeSection` 各标准规范查阅与逐档编辑、`showAddProvinceModal` 自定义省份录入
- 底部「版本 v2.4.0 / 隐私政策 / 用户协议 / 帮助中心」弱按钮（版本号改为 `v1.0.0` 或保留，见 §8 决策点）
- 页脚 `© 2024 工程造价助手`（年份可更新为 2026）

---

## 六、代码改动清单

| 文件 | 改动 |
|---|---|
| `src/types.ts` | 删除 `CalculationRecord`；新增 `HistoryModule` / `HistoryRecord` |
| `src/App.tsx` | ① 删除 `MOCK_RECORDS`；② 新增历史与默认省份的 localStorage 工具函数；③ 4 个测算回调补 `pushHistory(...)`；④ 重写「历史」页面 JSX（搜索/筛选/卡片/详情浮层/撤销删除）；⑤ 重写「我的」页面：删账号 UI、加默认省份卡；⑥ 4 个省份 `useState` 改惰性初始化读默认省份；⑦ 清理不再使用的 icon 导入 |
| `src/data/*.ts` | **不改动** |
| `server.ts` | **不改动**（历史与默认省份纯前端 localStorage，不上后端） |
| `src/services/calculationService.ts` | **不改动** |

> 历史记录与默认省份均为**本机存储**，不做云同步、不做多设备同步（与现有 `cost_calculator_custom_provinces` / `cost_calculator_custom_design_provinces` 做法一致）。

---

## 七、验证用例

### 7.1 历史页面
| # | 用例 | 期望 |
|---|---|---|
| 1 | 依次测算 4 个模块各 1 次 | 历史页出现 4 张卡片，模块色标/图标正确，时间倒序 |
| 2 | 点击「工程设计费」卡片 | 浮层显示该条报告全文，与测算时一字不差，标题金额 = 卡片金额 |
| 3 | 点「复制」 | 剪贴板内容 = 报告全文，按钮短暂变「已复制」 |
| 4 | 点删除 → 3 秒内点「撤销」 | 卡片恢复，条数不变 |
| 5 | 点删除 → 不做操作 | 卡片消失，刷新后仍消失（已持久化） |
| 6 | 搜索「市政」 | 只剩 title/subtitle 含「市政」的卡片 |
| 7 | 筛选「工程可研费」 | 只剩可研卡片，chip 上条数正确 |
| 8 | 删除全部记录 | 显示空状态 + 「前往计算器 ⇒」可跳转 |
| 9 | 连续快速点 2 次「设计费生成」 | 只产生 1 条记录（重复报告文本去重） |
| 10 | 造 205 条记录 | 只保留最新 200 条 |
| 11 | 刷新页面 / 重开 App | 记录仍在 |
| 12 | `localStorage` 塞入非法 JSON 后启动 | 不白屏，历史为空 |
| 13 | 点「重新测算」 | 跳到对应模块并回填参数（有 payload 时） |

### 7.2 我的页面
| # | 用例 | 期望 |
|---|---|---|
| 14 | 打开「我的」 | 无头像卡、无「退出登录」、无「账号设置」入口 |
| 15 | 设「工程监理费」默认省份 = 河南省，刷新页面 | 计算器 → 监理费，第 1 步默认选中「河南省」 |
| 16 | 同上，检验造价咨询费 / 可研费 / 设计费 | 各自默认省份生效 |
| 17 | 点「恢复默认」 | 4 项回到 湖南省/湖南省/湖南省/全国 |
| 18 | 默认省份选了某自定义省份，再把该自定义省份删除 | 该模块默认省份自动回落到第一个内置省份，不报错 |
| 19 | 检查「计算器」4 个模块 | 计算逻辑、报告格式、金额**与改动前完全一致**（回归） |
| 20 | `npx tsc --noEmit` | 无新增错误（仅剩既有 `@types/react` 噪声） |
| 21 | 浏览器控制台 | 无报错 |

---

## 八、需你确认的决策点

请逐条回复（可直接说「按建议」）：

1. **历史记录上限**：建议 **200 条**（超出自动丢弃最旧）。是否合适？
2. **「重新测算」按钮**：需要额外保存**入参快照 `payload`**（会占一点存储，但可以一键回填）。是否要？
   - ① 要（推荐，体验好）；② 不要（只做跳转，实现更简单）
3. **同一参数重复测算**：建议每次测算都留一条记录（保留完整操作痕迹），仅对「同一秒内报告文本完全相同」去重。是否同意？
4. **默认省份生效时机**：建议**下次启动生效**，不强制打断当前已选省份。是否同意？
5. **「我的」页面账号部分**：
   - ① 全部删除，页面顶部改为「默认省份设置」卡（推荐）
   - ② 保留一个纯展示的「工程造价助手 v1.0.0」信息卡 + 默认省份卡
6. **是否需要清空按钮**：历史页顶部是否加「清空全部」按钮？（建议加，带二次确认）
7. **版本号**：`v2.4.0` / `© 2024` 是否顺手更新？
8. **是否要类型筛选之外的「按省份筛选」**？建议不做，保持简洁。

---

## 九、实施顺序（确认后执行）

1. `src/types.ts`：类型定义
2. `src/App.tsx`：localStorage 工具函数（历史 + 默认省份）
3. `src/App.tsx`：4 个测算回调补 `pushHistory`
4. `src/App.tsx`：重写「历史」页面 JSX
5. `src/App.tsx`：4 个省份 `useState` 改为读默认省份
6. `src/App.tsx`：重写「我的」页面（删账号 UI + 加默认省份卡）
7. `npx tsc --noEmit` + 浏览器端到端回归（§七 全部用例）
8. 清理临时文件 → 提交归档
