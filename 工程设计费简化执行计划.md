# 工程设计费模块 · 简化执行计划（去省级区分）

> 目标：**不再按省份区分计算规则**，只保留「全国（计价格〔2002〕10 号）」一套规则；
> 省份名称与「+ 新增自定义省份」入口保留，便于日后扩展。

---

## 一、现状问题

当前设计费模块内置 **6 套省级口径**（全国 / 湖南 / 广东 / 黑龙江 / 新疆 / 海南），
每套口径都带自己的**基价表、复杂程度表、修正系数表、阶段比例表**，导致：

| 位置 | 复杂度 |
|---|---|
| 步骤 1 · 测算省份 | 下拉 6 项，每项规则不同 |
| 步骤 4 · ① 复杂程度 | 3 项（国标）或 4 项（湖南/黑龙江）或 3 项（新疆，且系数含义不同） |
| 步骤 4 · ② 修正系数 | 国标仅 1 项；**湖南有 12 项**（绿色建筑 3 档 + BIM 3 档 + 被动式 + 装配式 + EPC …） |
| 步骤 4 · ⑥ 阶段比例 | 国标 16 项 / **湖南 13 项** / 黑龙江 10 项，且随省份整表切换 |
| 数据维护 | `designStandards.ts` 中 5 省数据 + 4 套复杂程度表 + 3 套阶段比例表 |

结果：同一屏内选项数量随省份剧烈变化，页面臃肿，而绝大多数使用场景其实就是国标。

---

## 二、方案总览

**简化为：1 套内置规则 + N 个用户自定义省份**

```text
1. 测算省份 → 2. 工程类型 → 3. 计费基数 → 4. 阶段与调整系数 → 5. 测算报告
      ↑
   下拉 = 「全国（计价格〔2002〕10号）」+（省份名称）+「+ 新增自定义省份…」
```

| 项 | 处理 |
|---|---|
| 内置规则 | **只保留「全国（计价格〔2002〕10 号）」**（基价表 18 档 / 封顶 1.6% / 复杂程度 Ⅰ0.85 Ⅱ1.00 Ⅲ1.15 / 阶段比例表 7.2-1 / 修正系数：改扩建 1.1~1.4 / 其他设计收费 4 项） |
| 湖南、广东、黑龙江、新疆、海南 | **计算规则数据全部删除**（含各自的复杂程度表、阶段比例表、湖南专属修正系数表） |
| 省份名称 | 保留在下拉中（见第三章 A/B 方案） |
| 「+」入口 | 保留并**实现为可用的「新增自定义省份」弹窗**（沿用造价咨询费已有模式，`localStorage` 持久化） |
| 步骤 4 页面 | 全国口径下天然瘦身：修正系数 1 项、其他设计收费 4 项、复杂程度 3 项 |

---

## 三、需要你确认的关键点（A / B 二选一）

你说「保留现有省份的名称」，有两种读法，请选一个：

### ✅ A 方案（推荐 · 字面最贴合）

下拉框内容：

```text
全国（计价格〔2002〕10号）      ← 内置规则
湖南省
广东省
黑龙江省
新疆维吾尔自治区
海南省
+ 新增自定义省份...
```

- 选中**任一省份名称**时，**统一套用全国规则**计算（即"不再进行各个省份的区分"）
- 省份仅用于**报告标注**，报告第 1 行写：
  `1. 测算省份：湖南省 ｜ 适用规则：全国（计价格〔2002〕10号）`
- 好处：以后某省有了正式规则，只需通过「+」录入倍数即可生效，无需改代码

### B 方案

下拉框内容：

```text
全国（计价格〔2002〕10号）
+ 新增自定义省份...
```

- 其他省份名称从下拉中移除（仅作为**已归档参数**保留在数据文件的注释清单里，见第五章）

---

## 四、「+ 新增自定义省份」弹窗设计

沿用造价咨询费 `showAddProvinceModal` 的交互与样式，字段大幅精简：

| 字段 | 说明 | 默认值 |
|---|---|---|
| 1. 省份名称 | 文本，如「湖南省」 | — |
| 2. **基价倍数** | 相对计价格〔2002〕10 号标准的倍数 | 1.00 |
| 3. **封顶费率** | 计费额 > 2000000 万元时按此费率 | 留空则 = 1.6% × 倍数 |
| 4. 工程复杂程度 | ☐ 增加「Ⅳ级（特别复杂）」选项，默认 1.30（可改） | 继承国标 0.85/1.00/1.15 |
| 5. 各阶段工作量比例 | ☐ 按复杂程度等级自动联动；否则手工填 方案/初步/施工图 三项 | 继承国标表 7.2-1 |
| 6. **高级：逐档编辑基价表**（默认折叠） | 只读显示「国家标准 × 倍数」的预览表；点「自定义」后可逐档改数值 | 自动生成 |

> **为什么用"倍数"而不是整张表？** 因为已验证各地基价表与国家表的比例关系是固定的
> （湖南 ×1.20、广东 ×0.85、黑龙江 ×1.25/1.20/1.15、新疆/海南 ×1.00）。
> 以后扩展一个省，通常只需填「名称 + 倍数 + 封顶费率」三栏。

保存后写入 `localStorage('cost_calculator_custom_design_provinces')`，
出现在步骤 1 下拉中并自动选中；可在「我的 → 标准配置 → 工程设计费规范」中查看/删除。

### 📌 已归档的省级参数（供日后一键恢复或手工录入参考）

| 省份 | 基价倍数 | 封顶费率 | 复杂程度 | 备注 |
|---|---|---|---|---|
| 湖南省 | ×1.20 | 1.92% | Ⅰ/Ⅱ/Ⅲ/Ⅳ = 0.85/1.00/1.15/**1.30** | 另有 12 项专属修正系数（绿色建筑/BIM/被动式/装配式/EPC…） |
| 黑龙江省 | ×1.25（≤1亿）/ ×1.20（2–10亿）/ ×1.15（≥20亿） | 1.75% | Ⅰ/Ⅱ/Ⅲ/Ⅳ | **分段倍数**，需用「高级 → 逐档编辑」表达 |
| 广东省 | ×0.85 | 1.36% | Ⅰ/Ⅱ/Ⅲ | — |
| 新疆 | ×1.00 | 1.60% | 1.00/1.15/1.30 | 复杂程度等级编号不同 |
| 海南省 | ×1.00 | 1.60% | Ⅰ/Ⅱ/Ⅲ | 基本设计费含概算编制费 6.5% |

---

## 五、代码改造清单

### 1. `src/data/designStandards.ts`

| 动作 | 内容 |
|---|---|
| **删除** | `DESIGN_STANDARDS` 中 `湖南省` / `广东省` / `黑龙江省` / `新疆` / `海南省` 五条 |
| **删除** | `COMPLEXITY_4`、`COMPLEXITY_XJ`、`PHASES_HUNAN`、`PHASES_HLJ`、`FACTORS_HUNAN` |
| **保留** | `AXIS_NAT` / `PRICES_NAT` / `COMPLEXITY_3` / `PHASES_NAT` / `FACTORS_COMMON` / `OTHER_FEES_COMMON` / `DESIGN_PROFESSION_GROUPS` / `DESIGN_MODES` |
| **新增** | `DESIGN_PROVINCE_NAMES`：下拉展示用的省份名称清单（对应 A/B 方案） |
| **新增** | `buildStandardFromMultiplier({ name, multiplier, capRate, complexity, phases, axis, prices })` → 由倍数生成完整 `DesignStandard`（基价 = 国标 × 倍数，四舍五入 4 位；封顶 = 1.6% × 倍数；`source` 自动写「自定义省份：相对计价格〔2002〕10 号标准 ×N」） |
| **新增** | `DESIGN_ARCHIVED_PROVINCES` 注释块：记录第四章表格，便于日后恢复（不参与运行） |
| 类型 | 新增 `CustomDesignProvince`（name / multiplier / capRate / complexity4 / phaseMode / phases / axis / prices） |

### 2. `src/App.tsx`

| 动作 | 内容 |
|---|---|
| **新增 state** | `customDesignProvinces`（`localStorage` 初始化，key `cost_calculator_custom_design_provinces`）、`showAddDesignProvinceModal`、`newDesignProvName` / `newDesignProvMultiplier` / `newDesignProvCapRate` / `newDesignProvComplexity4` / `newDesignProvPhaseMode` / `newDesignProvPhases` / `newDesignProvAdvanced` 等表单 state |
| **改造** | `getDesignStandard()`：命中自定义省份 → `buildStandardFromMultiplier(...)`；否则返回 `DESIGN_STANDARDS['全国']` |
| **改造** | 步骤 1 下拉：`全国（计价格〔2002〕10号）` + 省份名称（A 方案）+ 自定义省份 + `+ 新增自定义省份...`（`value="ADD_CUSTOM"` 时打开弹窗，与造价咨询费一致） |
| **改造** | 省份切换时的重置逻辑：不再重置复杂程度（各口径等级已统一为 Ⅰ/Ⅱ/Ⅲ，新疆口径已移除），仅重置自定义省份相关项与结果 |
| **新增** | 「新增自定义省份」弹窗 JSX（含基价表预览、删除自定义省份） |
| **改造** | 标准配置面板「工程设计费规范」：改为「全国基价表」+「自定义省份列表（可删除）」 |
| **精简（可选）** | 步骤 4 的 ⑥ 阶段比例：保留 16 项国标预设但改为**可折叠分区**；若你希望更进一步，可精简为 8 项常用预设（建筑与室外 Ⅰ/Ⅱ/Ⅲ级、住宅小区、住宅工程、园林绿化、人防、市政公用）—— 见第八章问题 3 |
| **保留** | `extractFeeOnly` 的 `项目总设计费用：`、`goToReportPage()`、报告页设计费卡片 |

### 3. `server.ts`

`POST /api/calculate-design` 的省份查找逻辑调整：

```text
优先使用请求体中的 standard 快照（自定义省份由前端 buildStandardFromMultiplier 生成后传入）
未传 standard 时回退到内置 DESIGN_STANDARDS[province]（仅剩「全国」）
```

这样后端仍是**唯一报告生成器**（`calculateDesign` 不变），自定义省份也能走后端；
后端不可用时前端本地回退逻辑保持不变。

### 4. 不变的部分

- `calculateDesign()` 计算公式、`locateDesignBracket()`、`mergeAdditionalFactors()`、报告模板 —— **全部不动**
- 步骤 2「工程类型」（国家附表二 7 大类 22 项 + 自定义）、步骤 3「计费基数」
- 步骤 4 的 ①复杂程度 ②修正系数 ③计费模式 ④其他设计收费 ⑤浮动幅度

---

## 六、实施后效果对比

| 项 | 现在 | 简化后 |
|---|---|---|
| 内置规则数 | 6 | **1** |
| 步骤 1 下拉项 | 6 | 1 + 5 个省份名称（A 方案）+ 自定义 + 「+」 |
| 步骤 4 ② 修正系数最大项数 | **12**（湖南） | **1**（改扩建） |
| 步骤 4 ⑥ 阶段比例预设 | 16 / 13 / 10（随省份变） | 固定 16（国标表 7.2-1） |
| 复杂程度等级 | 3 或 4（随省份变） | 固定 3（+ 自定义可选 4） |
| 数据文件行数 | ~700 | 约 **-45%** |
| 扩展新省份成本 | 改代码 + 找齐 4 张表 | 界面填「名称 + 倍数 + 封顶费率」 |

---

## 七、验收方案

1. **回归**：全国口径全部用例重跑（6000 万元 = 192.4667 万元；2,500,000 万元超顶档 = 40,000 万元；改扩建 1.2 + EPC 1.1 合并 = 1.3；浮动 −20%；优质优价 +25%；Ⅲ级 1.15；标准复用模式 ×0.3）
2. **自定义省份**：新建「湖南省（×1.20 / 1.92%）」→ 6000 万元应得 **230.9667 万元**（与原内置湖南一致）；新建「广东省（×0.85 / 1.36%）」→ 应得 **163.5967 万元**
3. **持久化**：刷新页面后自定义省份仍在；删除后消失
4. **后端接口**：带 `standard` 快照调用 `/api/calculate-design` 正常返回；不带快照且传 `province: '全国'` 也会兜底
5. **本地回退**：关闭 3001 端口重复第 2 项
6. `npx tsc --noEmit` 通过（过滤既有 `@types/react` 告警）
7. 控制台无报错；确认 5 个已删除省份不再出现在任何下拉中

---

## 八、需要你确认的 4 点

| # | 问题 | 我的建议 |
|---|---|---|
| 1 | 省份下拉按 **A 方案**（保留省份名称、统一套用全国规则）还是 **B 方案**（只留「全国」+「+」）？ | ✅ **A 方案** |
| 2 | 「+」是否实现为**可用的自定义省份弹窗**（含倍数/封顶费率/复杂程度/阶段比例/高级逐档编辑）？ | ✅ 实现（否则「以后可以扩展」落不了地） |
| 3 | 步骤 4 的「⑥ 各阶段工作量比例」16 项预设，是否**精简为 8 项常用预设**？ | ✅ 精简（进一步降低页面复杂度） |
| 4 | 是否把步骤 4 的 ①~⑥ 分区改为**可折叠**（默认只展开 ①复杂程度、⑤浮动幅度）？ | ✅ 折叠（这是"页面过于复杂"的主要来源） |

**无需回答、实施时直接执行：**
- 删除 5 省规则数据及其 4 套附属表
- `designStandards.ts` 顶部补充「已归档省级参数」注释块
- `/api/calculate-design` 支持 `standard` 快照传入
- 保留章节：步骤 2/3 不动，`calculateDesign()` 计算与报告模板不动
