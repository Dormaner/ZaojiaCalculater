# 工程造价计算器 · 代码整体整理与精简执行计划

> 目标：**在不改变任何现有功能的前提下**，整理结构、删除废码、消除前后端重复、修复若干小缺陷。
> 本文档先确认、后执行；确认后再动手，并按阶段分次提交。

---

## 一、体检结论（本次实测数据）

| 文件 | 行数 | 体积 | 备注 |
|---|---:|---:|---|
| `src/App.tsx` | 5 414 | 327 KB | 单文件包含全部页面、状态、四套向导逻辑 |
| `server.ts` | 1 050 | 53 KB | 与前端重复实现造价咨询费全套逻辑 |
| `src/data/designStandards.ts` | 658 | 30 KB | 前后端共用（唯一数据源） |
| `src/data/feasibilityStandards.ts` | 586 | 25 KB | 前后端共用 |
| `src/data/supervisionStandards.ts` | 333 | 20 KB | 前后端共用 |
| `src/services/calculationService.ts` | 93 | 3.7 KB | **仅被 import、四个导出全部未被调用** |
| `src/types.ts` | 66 | 1.7 KB | |
| `src/index.css` / `main.tsx` / 配置文件 | 37 / 9 / 38 | 小 | |

**基线状态**
- `npm run build`：**通过**（2078 模块，522 KB JS / 148 KB gzip；有 chunk >500 KB 提示）
- `npx tsc --noEmit`：**4 个错误**，全部为 `TS2503: Cannot find namespace 'React'`（`App.tsx` L1478、L1481、L1493、L1497 的 `React.TouchEvent` / `React.MouseEvent`）——因为 `node_modules/@types` 下**没有 `@types/react`、`@types/react-dom`**
- 后端 `localhost:3001` 通常未启动，前端走本地回退分支（现状正常）

**已确认的废弃代码（实测引用次数）**

| 位置 | 标识 | 证据 |
|---|---|---|
| `src/services/calculationService.ts` 全文 | `interpolate` / `calculateDesignFee` / `calculateProgressiveFee` / `calculateConsultingFee` | `App.tsx` L46–49 只 import 了 3 个，**全文 0 次调用**（页面里用的是同名的本地函数 `calculateProgressiveFeeWanyuan`） |
| `App.tsx` L348 | `FeeSection` | 定义 1 次、**0 次引用** |
| `App.tsx` L194、L437 | `FeeItem` | 仅被上面的 `FeeSection` 引用（连带废弃） |
| `App.tsx` L1824 | `deletePreset` | 定义 1 次、**0 次调用** |
| `App.tsx` L1850 | `displayStandard` | 只赋值、**从不读取** |
| `App.tsx` L2714 | `goToReportPage` | 定义 1 次、**0 次调用**（唯一作用是 `setActiveTab('report')`） |
| `App.tsx` L901 | `lastCalculation` / `setLastCalculation` | state 声明后**从未读写** |
| `App.tsx` L1280 区 | `expandedPref` / `setExpandedPref` | state 声明后**从未读写** |
| `App.tsx` 预设相关 | `showSavePresetModal`、`newPresetName`、`showOverwriteWarning` | 均为「声明 + setter」，无读取/调用（待逐项二次确认） |
| `App.tsx` L4852 起 ≈200 行 | `activeTab === 'report'` 汇总页 | 唯一入口 `goToReportPage` 无人调用 → **当前不可达** |
| 根目录 | `extracted_*.txt`、`extracted_line(s).json`、`line_157.json`、`metadata.json`、`restore.cjs` | 建库期一次性产物（已被 git 跟踪） |
| 根目录 | `咨询费结果.docx`、`_tsc.txt` | 临时文件（未入库） |

**已知的真实小缺陷**
1. 造价咨询费步骤 1 选「＋ 新增自定义省份…」后**看不到弹窗**——该 Modal 的 JSX 位于「我的」页签的渲染分支内（`App.tsx` L5443 区），只有切到「我的」页才渲染。
2. 后端未启动时，每一步测算都会打印一段完整的 `console.warn` 堆栈，控制台噪音大。
3. `package.json` 的 `clean` 脚本是 `rm -rf dist`（Windows 下不可用）；`name` 仍是 `react-example`。

---

## 二、执行计划（分 7 个阶段，逐阶段验收）

### 阶段 0　建立回归护栏（先做，否则后面无法证明"功能没变"）
| 动作 | 说明 |
|---|---|
| 固化 4 个回归算例 | 设计费 2000 万 Ⅱ级+改扩建1.2+室内装修1.5+总体设计费5% → **¥1,272,705**；可研费 湖南 8000 万 两项服务 行业0.8 复杂1.0 → **¥224,914**；监理费 湖南 6000 万 住宅+项目管理20% → **¥2,256,000**；造价咨询费 湖南 全过程B型 6000 万 → **¥760,000** |
| 复用自动走查脚本 | 把现有 `_demo_record.cjs` 的 `DRY=1` 走查（含步骤断言）作为"功能未变"的自动检查，整理前后各跑一次比对 |
| 保存报告文本快照 | 整理前先把 4 份报告全文导出到 `tmp_reports_before/`（每阶段后逐字 diff） |

### 阶段 1　类型与构建基线修复（零功能影响）
1. 安装 `@types/react`、`@types/react-dom`（devDependencies，React 19 对应版本）
2. 修 4 处 `React.xxxEvent`：改为 `import type { TouchEvent, MouseEvent } from 'react'` 或 `import type * as React from 'react'`
3. `package.json`：`clean` 改为跨平台写法（`node -e "fs.rmSync('dist',{recursive:true,force:true})"`）；`name` 改为 `cost-calculator`
4. **验收**：`npx tsc --noEmit` → 0 错误；`npm run build` → 通过

### 阶段 2　删除已确认的废弃代码（零功能影响）
- 删除 `src/services/calculationService.ts` 整文件 + `App.tsx` 中对应 import
- 删除：`FeeSection`、`FeeItem`、`deletePreset`、`displayStandard`、`goToReportPage`、`lastCalculation`、`expandedPref`、以及预设功能中确认无引用的 state/函数（逐个用「引用计数=1 且无 JSX 引用」判定）
- 清理 `lucide-react` 未使用图标导入（用一次性 `tsc --noUnusedLocals` 扫描后统一删）
- 清理 `types.ts` 中无引用的类型（如 `FeeBreakdown`，确认后再删）
- **验收**：`tsc --noUnusedLocals --noUnusedParameters` 报错数显著下降；DRY 走查全绿；4 份报告文本与阶段 0 快照逐字一致

### 阶段 3　消除前后端重复（结构性精简，重点）
现状：造价咨询费的**费率表 + 类别系数 + 服务项目清单 + 差额累进算法 + 报告文本生成器**在 `App.tsx`（约 500 行常量 + 约 470 行计算）与 `server.ts`（约 600 行）各存一份；`calculateProgressiveFeeWanyuan` 等同名双份。

方案：新增 `src/data/consultingStandards.ts`，比照现有三个标准模块的做法，成为造价咨询费的**唯一数据源**；`App.tsx` 与 `server.ts` 均改为 import。
- 迁移顺序：常量表 → 纯计算函数 → 报告文本生成器 → 两侧接线
- **硬性要求**：函数签名不变、报告文本逐字节不变（用 14 个单项服务 + 8 个全过程/跟踪审计项目逐一生成报告比对）
- 预计：`App.tsx` 减少约 500～700 行，`server.ts` 减少约 550 行

### 阶段 4　`App.tsx` 结构拆分（只搬不改，可选）
5 414 行单文件，建议按"原样搬运"原则拆分：

```
src/
├─ App.tsx                    主组件：三页签路由 + 顶层状态编排
├─ components/
│   ├─ layout/ 底部导航、模块标签、步骤条
│   ├─ design/     4 个步骤组件 + 报告卡片
│   ├─ feasibility/ 同上
│   ├─ supervision/ 同上
│   ├─ consulting/  同上
│   ├─ history/     历史列表、筛选、报告浮层、确认弹窗
│   └─ profile/     默认省份设置、自定义省份弹窗
├─ hooks/   useHistory、useDefaultProvinces、useSwipe、useCustomProvinces
├─ utils/   format.ts（fmtAmount / fmtHistoryTime / extractTotalYuan / formatNum）
└─ data/    4 个标准模块（design/feasibility/supervision/consulting）
```
⚠️ 风险最高、收益主要是可维护性。建议**分 3～4 次提交**，每次只搬一个模块并跑一次回归。**若只想"精简"，此阶段可整段跳过。**

### 阶段 5　小缺陷修复（不改交互逻辑）
| 编号 | 缺陷 | 修法 | 功能影响 |
|---|---|---|---|
| B1 | 造价咨询费「＋ 新增自定义省份…」弹窗不显示 | 把该 Modal 从「我的」页分支移到与页签无关的顶层位置 | 无（只是让既有按钮生效） |
| B2 | 后端不可用时逐步打印完整 warn 堆栈 | 改为首次静默降级 + 一次性简短提示 | 无 |
| B3 | 剪贴板降级路径 | 核对 `execCommand` 兜底是否覆盖失败分支，仅补兜底 | 无 |
| B4 | `alert()` 提示（如"请选择工程类别"）体验差 | **暂不改**（会改变交互），留待你决定 | — |

### 阶段 6　仓库整理
- 删除：`extracted_*.txt`、`extracted_line(s).json`、`line_157.json`、`metadata.json`、`restore.cjs`、`咨询费结果.docx`、`_tsc.txt`（如需留档，改为移入 `docs/`）
- `.gitignore` 增补 `_tsc.txt`、`tmp_reports_*/`
- 保留 `_gen_*.cjs`、`_check_manual.cjs`、`_demo_record.cjs`（手册与视频可复现）

### 阶段 7　验收与存档
1. `npx tsc --noEmit` → 0 错误；`npm run build` → 通过
2. `DRY=1 node _demo_record.cjs` → 全部 `[OK]`（四模块走查 + 断言）
3. 4 份报告文本与阶段 0 快照**逐字一致**
4. 手工过一遍 5 个界面（4 个模块 + 历史 + 我的），截图目视对比
5. 分阶段提交，提交信息形如「代码整理：删除废弃代码与重复实现0922」

---

## 三、需要你决策的 3 件事

| # | 事项 | 选项 |
|---|---|---|
| 1 | 是否需要**阶段 4（拆分 App.tsx）** | ① 做（结构最清晰，回归面大）② 不做，只做 1/2/3/5/6（改动最小、风险最低）——**建议 ②** |
| 2 | `activeTab === 'report'` 的**汇总页**（约 200 行，当前无入口） | ① 删除 ② 保留但不动 ③ 恢复入口（属于新增功能）——**建议 ①** |
| 3 | 报告文本中的**政府文件名称**是否同步更正（设计费应为《工程设计收费标准》、造价咨询费应按原文名称） | ① 一起改（会改变报告输出文字）② 保持现状——**建议 ①，但需要你确认** |

> 另：此前核对政府文件时发现「湖南省全过程造价咨询 A～D 型费率与随附文件附表 6 不一致」。修改它会**改变计算结果**，已超出"不改功能"的范围，本计划**不动**，如需修正是另一个独立任务。

---

## 四、风险控制

| 措施 | 说明 |
|---|---|
| 小步提交 | 每阶段一次提交，任一阶段回归失败可单独回滚 |
| 文本比对 | 报告文本是功能的最终体现，整理前后做逐字 diff，而不是只看截图 |
| 不动计算参数 | 所有费率表、系数、档位的数值一律不修改（除非你确认第 3 项决策） |
| 不改交互 | 不调整步骤、按钮、弹窗的触发条件与提示方式（B1 属于"让既有按钮可用"的缺陷修复） |
| 保留回退 | 后端与前端本地计算的"双通道"结构保持，仅合并重复实现，不删任何一条通道 |
